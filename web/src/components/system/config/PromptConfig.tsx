'use client';

import PromptConfigShared from '@/components/shared/prompt';

/**
 * Prompt Configuration component for the system settings page 
 */
export default function PromptConfig() {
  return <PromptConfigShared isWorkspace={false} />;
}
