{
  "name": "@mandrake/langchain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist coverage .turbo tsconfig.tsbuildinfo",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@langchain/anthropic": "^0.3.13",
    "@langchain/core": "^0.3.38",
    "@langchain/openai": "^0.3.17",
    "@mandrake/mcp": "^0.0.1",
    "@mandrake/storage": "^0.0.1",
    "@mandrake/types": "^0.0.1",
    "dotenv": "^16.4.7"
  }
}
