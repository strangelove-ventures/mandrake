{
  "name": "@mandrake/storage",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "prisma generate",
    "test": "jest",
    "build": "tsc --build",
    "clean": "rm -rf dist coverage .turbo node_modules/.prisma tsconfig.tsbuildinfo",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@langchain/core": "^0.3.32",
    "@mandrake/types": "^0.0.1",
    "@prisma/client": "6.2.1",
    "@prisma/extension-pulse": "^1.2.0",
    "langchain": "^0.3.12"
  },
  "devDependencies": {
    "prisma": "6.2.1",
    "typescript": "^5.3.3"
  }
}
